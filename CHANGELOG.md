# 更新日志

## 0.2.2（2026-09-17）

- **修复：补投没有正确屏蔽全局屏蔽的用户（`[chat] ban_user_id` 形同虚设）。**
  根因是名单镜像只在 `on_load` / 自身配置热更新时尝试**一次**，而本插件的 `on_load`
  早于同一 Runner 组里的 Napcat 适配器注册完成——宿主的 `config.get_plugin` 此时
  解析不到适配器目录、返回空 dict，镜像必然失败（实测日志：`配置缺少 [chat] 节`
  34 次、`已自动镜像` 0 次）。失败后又**没有任何重试**，于是永久回退到手动名单；
  而手动名单默认全空（`group/private = disabled`、`ban_user_id = []`），
  结果就是**范围过滤整体失效**：被全局屏蔽的用户贴表情 / 撤回 / 设精华的通知照常补投。
- **改为按需 + 定期重新镜像**（`_ensure_filter_fresh()`，在每次补投决策前调用）：
  - 从未镜像成功 → 每次决策都重试，直到拿到适配器名单，**绝不在「名单为空」状态下放行**；
  - 已有快照但过期 → 到 `filter.mirror_refresh_seconds`（新配置，默认 60 秒）后重新读取，
    适配器侧新增/移除的屏蔽用户与群名单会自动生效（旧版必须手动重载本插件）；
  - 刷新失败但有旧快照 → 按 `MIRROR_RETRY_INTERVAL_SEC`（10 秒）退避，期间继续用旧快照过滤，
    而不是退化成「不过滤」。
- **新增配置 `filter.mirror_refresh_seconds`**（默认 60；`0` = 只在加载与配置热更新时镜像一次）。
- **新增配置 `filter.mirror_fail_closed`**（默认关）：镜像读不到适配器名单时拒绝一切补投，
  宁可漏投也不越权。默认关闭以保持旧版「回退手动名单」的行为不变。
- **失败可诊断、且不再刷屏**：镜像失败改为限流告警（`MIRROR_FAIL_LOG_INTERVAL_SEC` = 300 秒），
  并打印**读到的顶层键**——若列出的键是本插件自己的（`relay` / `filter` 等），说明
  `filter.adapter_plugin_id` 填错；若是空列表，则确认是启动竞态，会自动恢复。
- 版本号与 `config_version` 同步升至 `0.2.2`；新增两个配置项均带中文 `label` / `hint`（hint ≤15 字）。
- 回归：`过滤口径与适配器一致` 9 个用例、`启动竞态 / 名单刷新 / fail-closed / 默认行为不变` 4 个场景全过。

## 0.2.1

为全部配置项补充/完善了用户友好的中文注释与说明（悬停提示），完善配置节说明；插件功能与行为不变。

## 0.2.0（2026-09-04）

- **自动镜像官方 Napcat 适配器 `[chat]` 黑白名单**：新增 `sync_from_adapter`（默认开启），插件加载/自身配置热更新时通过 `config.get_plugin` 读取 `maibot-team.napcat-adapter` 的 `[chat]` 节（群/私聊白黑名单 + `ban_user_id`），消除手动镜像名单的越权补投风险；失败自动回退手动名单。
- 范围过滤新增**私聊名单维度**（`private_list_mode` / `private_list`），`friend_recall` 补投遵循适配器私聊名单口径。
- manifest：新增 `config.get_plugin` 能力声明；适配器名单读取为软依赖（未装官方适配器时回退手动名单）。**升级需完整重启 MaiBot**。

## 0.1.0（2026-09-03）

- 首个版本：影子中继上线。
- 独立 OneBot WS 连接协议端，只订阅四类易丢通知（表情回应/群撤回/好友撤回/精华）。
- `chat.receive.before_process` 只读 Hook 登记适配器已送达的事件摘要；去抖窗口后缺位即补投。
- 补投 `dedupe_key` 使用事件摘要（`shadow:{sha1}`），与适配器键空间隔离。
- 昵称/群名富化（走自有 WS 查询 + 缓存），兼容下游表情回应翻译插件。
- 上游修复 #97 后自动退化为 no-op；支持 SnowLuma 适配器兼容字段与"独占中继"模式。
